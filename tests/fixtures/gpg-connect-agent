#!/bin/sh
# Test shim: shadows the real `gpg-connect-agent` on PATH in integration
# tests so `sm gpg` talks to the scripted stand-in. See
# fake-gpg-connect-agent.sh.
exec "$(dirname "$0")/fake-gpg-connect-agent.sh" "$@"
